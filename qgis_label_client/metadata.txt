[general]
name=CVI Label Client
qgisMinimumVersion=3.44
qgisMaximumVersion=4.99
supportsQt6=True
description=Connect QGIS to an OGC API - Features labeling backend with Google sign-in, editable class layers and original attribute columns on supported servers, local edit recovery, and historical views.
version=0.2.1
author=Compute Visibility Institute
email=opensource@example.org

about=Connects QGIS to a labeling platform that serves OGC API - Features (Parts 1, 2 and 4).
    QGIS's native provider does all vector reading and editing, so this plugin only covers what
    QGIS cannot do on its own:
    .
    * signs in with Google in your browser and stores the resulting token in QgsAuthManager,
      so no credential is ever written into a .qgz - and renews that token in the background,
      because it lives about an hour and an editing session does not;
    * uses separate class layers and editable original attribute columns when supported,
      and falls back to legacy collections on older servers;
    * loads collections and the label class vocabulary from the backend, so forms and styling
      cannot drift from the server's class registry;
    * an as-of-date control on valid time, sent as the OGC datetime parameter or as a CQL2
      filter;
    * a historical view on transaction time - a read-only layer showing the labels as the team
      believed them at a chosen instant, including ones deleted since. Transaction time has no
      OGC parameter, so the instant is carried in the layer's landing URL. Historical
      collections and the plugin's native read-only flag prevent editing that view;
    * QA: a label's edit history, and a check for labels drawn outside any declared
      exhaustive survey extent;
    * a one-time bootstrap that publishes the vector layers already open in the project as
      the founding dataset, previewing the class mapping, the damaged names and the missing
      survey coverage before anything is sent; supported layer styles can initialize
      a class's style when the server is awaiting a QGIS upload;
    * keeps local edit recovery journals and offers Push all local after reconnecting.
    .
    Licensed under GPL v2 or later, as required for QGIS plugins. Account credentials stay
    in the QGIS authentication database. The backend URL is configurable.

tracker=https://github.com/Compute-Visibility-Institute/qgis-label-client/issues
repository=https://github.com/Compute-Visibility-Institute/qgis-label-client
homepage=https://github.com/Compute-Visibility-Institute/qgis-label-client

category=Web
icon=icons/cvi.svg
tags=ogc,oapif,ogc api features,wfs,labeling,annotation,cog,remote sensing,temporal

experimental=False
deprecated=False
server=False
hasProcessingProvider=no

changelog=0.2.1
    - Show class layers and original attribute columns in read-only mode on supported servers.
    - Avoid malformed track/class filters when opening attribute tables.
